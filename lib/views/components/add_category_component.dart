import 'package:flutter/material.dart';

class AddCategoryComponent extends StatelessWidget {
  const AddCategoryComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(16),
      child: Column(),
    );
  }
}
